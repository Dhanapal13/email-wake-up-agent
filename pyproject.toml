[project]
name = "email-wake-up-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "langchain-ollama>=1.1.0",
    "langgraph>=1.2.11",
    "uvicorn>=0.53.0",
]
